bind 127.0.0.1
port 6379
# Fail loudly rather than silently dropping queued jobs: a lost job is a missed
# order update, which reconciliation would eventually repair but not before
# somebody noticed.
maxmemory 512mb
maxmemory-policy noeviction
appendonly yes
appendfsync everysec
